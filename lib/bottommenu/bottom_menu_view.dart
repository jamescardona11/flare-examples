import 'package:flutter/material.dart';

class BottomMenuView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('BottomMenu View'),
      ),
    );
  }
}
